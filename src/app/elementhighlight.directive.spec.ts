import { ElementhighlightDirective } from './elementhighlight.directive';

describe('ElementhighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new ElementhighlightDirective();
    expect(directive).toBeTruthy();
  });
});
